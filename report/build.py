"""Aggregate eval/results/*.json + bench/results/*.json → comparison table.

TODO step 9: emit Markdown (for README) and JSON (for downstream tooling).
One row per predictor; columns match the README skeleton.
"""
